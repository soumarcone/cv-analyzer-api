# Testing Environment Configuration
# This file is loaded when APP_ENV=testing
# Safe to commit - contains only test/dummy values

APP_ENV=testing

# LLM Configuration (testing values)
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o
LLM_API_KEY=test-key-123

# API Configuration (testing values)
APP_API_KEY_REQUIRED=true
APP_API_KEYS=test-api-key-123,test-api-key-456

# Rate Limiting (disabled for deterministic tests)
APP_RATE_LIMIT_ENABLED=false
